# Claude session — 2026-07-08 — README rewrite

Context: the README had been frozen at its Week-1 state by Huiying's explicit
sequencing (submission doc last). Written today from an outline she approved
and corrected: she killed the "zero cost" claim (BYOK shifts cost to the
user's provider — it doesn't remove it), demanded the privacy story be stated
as verifiable mechanism rather than slogan, required a detailed usage guide,
and set the tone rule: no claim may sound airtight — risks and limitations get
their own sections, because a project like this always has holes.

## Shape

Try-it-in-60-seconds first (judges get one minute; give them one minute).
Then: problem / what it does / differentiation table (cost row rewritten
honestly) / AI architecture with the "AI curates, deterministic code builds"
story and its PR-visible origin / challenge fit / Built-with-Bob including the
real incidents (green-washed summaries, wrong-branch commits) as process
evidence / full usage guide (both provider routes, real costs, walk controls,
audio-guide semantics, export, troubleshooting) / Security & privacy stated as
data flow + threat model + what-we-verified vs what-we-can't-promise / testing
with the week-3 failure as the reason the E2E layer exists / known limitations
(leading with "one user has tested this end-to-end") / credits with the
CC0 sources and the AI-collaboration statement.

## Honesty checks applied

- Every relative link verified to exist; work-order count corrected against
  the actual file count (19, not the estimated ~15).
- No "production-ready/bulletproof/fully secure" language anywhere; the
  security section explicitly names what is NOT promised.
- Costs: provider-paid per generation, with our own trial-quota exhaustion
  mentioned as evidence it's real.

## Left for Huiying (marked as TODO comments in the file)

Hero image/GIF, demo video link, hosted-instance link (if she deploys), and a
read-through for voice — the words are drafted for her approval, not final.
The "three days" figure is used; she should confirm the framing.
